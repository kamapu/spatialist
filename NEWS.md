# spatialist v0.2

### New Features

- New fnction `mask_window()` for masking part of matrices used in `focal()`.

### Improvements

- Functions for raster data sets adapted to `SpatRaster` class of the package
  `terra`.

